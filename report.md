# 频繁项集挖掘算法比较报告

## 1. 项目概述

本项目实现了三种经典的频繁项集挖掘算法：Apriori、FP-Growth和Eclat，并在DBLP数据集的子集（随机10,000条记录）上进行了性能对比测试。通过对比不同算法在各种数据特征下的表现，分析了它们的优缺点和适用场景。

## 2. 算法实现

### 2.1 Apriori算法

Apriori算法基于"频繁项集的所有子集也是频繁的"这一性质，采用广度优先搜索策略，通过多次扫描数据库生成候选项集并验证其支持度。
`apriori.h`

### 2.2 FP-Growth算法

FP-Growth算法通过构建FP树来压缩数据集，避免了候选项集的生成，采用分治策略递归地挖掘频繁项集。
`fp_growth.h`

### 2.3 Eclat算法

Eclat算法采用深度优先搜索策略，使用垂直数据格式（TID-set）来计算项集的支持度，减少了数据库扫描次数。
`eclat.h`

## 3. 实验设计

### 3.1 数据集

使用DBLP数据集的子集（10,000条记录），最小支持度设置为5。根据不同的特征需求，构建了四种类型的数据集：

- **稀疏数据集**：仅包含作者和年份信息，通过`getSparseItems`函数生成，项集较少且分散
- **密集数据集**：包含所有可用字段（标题、作者、年份、会议等），通过`getDenseItems`函数生成，项集数量多且关联度高
- **长模式数据集**：包含标题分词（最多5个词）、会议信息和第一作者，通过`getLongPatternItems`函数生成，适合测试长模式挖掘能力
- **超密集数据集**：通过`getUltraDenseItems`函数生成，添加了多个随机数作为项，形成高度密集的数据特征

### 3.2 评估指标

- **时间效率**：算法运行总耗时（毫秒）
- **空间效率**：内存峰值使用量（MB）
- **算法效率**：候选项集数量（Apriori）或其他算法特定指标

## 4. 实验结果

| 算法（数据类型）    | 时间      | 空间（max） | 效率（候选项）                      |
| ------------------- | --------- | ----------- | ----------------------------------- |
| Apriori（稀疏）     | 165 ms    | 52.6875 MB  | 23681 + 1378 候选项                 |
| FP-Growth（稀疏）   | 121 ms    | 50.6094 MB  | 无需生成候选项                      |
| Eclat（稀疏）       | 135 ms    | 51.0625 MB  | -                      |
| Apriori（密集）     | 3617 ms   | 102.234 MB  | 43116 + 44850 + 496 候选项          |
| FP-Growth（密集）   | 237 ms    | 65.9688 MB  | 无需生成候选项                      |
| Eclat（密集）       | 401 ms    | 64.375 MB   | -                      |
| Apriori（长模式）   | 190242 ms | 490.344 MB  | 23278 + 737505 + 864 + 5 + 0 候选项 |
| FP-Growth（长模式） | 408 ms    | 77.7656 MB  | 无需生成候选项                      |
| Eclat（长模式）     | 2707 ms   | 58.7656 MB  | -                   |
| FP-Growth（超密集） | 43838 ms  | 1179.62 MB  | 无需生成候选项                      |
| Eclat（超密集）     | 4404 ms   | 234.125 MB  | -                    |


## 5. 结果分析

### 5.1 时间效率分析

- Apriori算法在所有场景下都表现最差，尤其在长模式和密集数据上耗时显著增加
- FP-Growth在稀疏和密集数据上表现最佳，但在超密集数据上性能下降
- Eclat算法在各种场景下都保持相对稳定的性能

### 5.2 空间效率分析

- Apriori算法随着数据复杂度增加，内存占用急剧上升
- FP-Growth在超密集数据上内存占用最高，达到1179.62MB，且特别容易出现内存溢出。
- Eclat算法在各种场景下内存占用相对较低且稳定

### 5.3 不同场景适用性分析

#### 稀疏数据场景
在稀疏数据上，三种算法性能差异不大，但FP-Growth略胜一筹。

#### 密集数据场景
在密集数据上，Apriori性能明显下降，而FP-Growth和Eclat仍然保持良好性能。

#### 长模式挖掘场景
在长模式数据上，Apriori算法几乎无法应用（耗时超过3分钟），而FP-Growth表现最佳。

#### 超密集数据场景
在超密集数据上，Eclat算法表现出最佳的综合性能，内存占用远低于FP-Growth。

## 6. 结论

- **Apriori算法**：适合稀疏数据挖掘。
- **FP-Growth算法**：适合稀疏和中等密度的数据集，但在超密集数据上内存消耗较大。
- **Eclat算法**：性能稳定，内存效率高，特别适合密集数据和需要控制内存使用的场景。
